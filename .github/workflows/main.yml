name: Main

on:
  push:
    branches: [ main ]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - id: step1
        run: |
          SECRET="secret"
          echo "::add-mask::$SECRET"
          echo "secret=$SECRET" >> $GITHUB_ENV
      - run: |
          echo "secret is $SECRET"